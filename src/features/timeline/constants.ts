/** タイムライン1日あたりのセル幅（px）。月表示・年表示（docs/spec.md §2.4）用。 */
export const DAY_WIDTH_PX = 32;

/**
 * 日（2週間）表示用のセル幅（px）。表示日数が14日と少なく余白ができやすいため、
 * 月表示より広げて画面を有効活用する。
 */
export const DAY_SCALE_WIDTH_PX = 64;

/** 階層ラベル列（サイドバー）の固定幅（px）。横スクロール時も左に固定表示される。 */
export const SIDEBAR_WIDTH_PX = 480;
